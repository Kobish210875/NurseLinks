/** Max wait for auth/session calls during page render (RSC). */
export const AUTH_SESSION_TIMEOUT_MS = 4_000;

/** Max wait for password sign-in server action. */
export const AUTH_SIGN_IN_TIMEOUT_MS = 12_000;

/** Max wait for admin email lookup on failed login. */
export const AUTH_LOOKUP_TIMEOUT_MS = 3_000;
