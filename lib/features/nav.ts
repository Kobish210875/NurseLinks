/** Jobs nav hidden until employer self-publish is viable; `/jobs` routes remain. */
export const SHOW_JOBS_IN_NAV = false;

/** Community discussion threads (ענפי שיחה). */
export const SHOW_DISCUSSIONS_IN_NAV = true;
