name: Restore Database from Backup

# MANUAL ONLY — never runs on a schedule.
# Triggered from the admin UI (Restore button) or directly from GitHub.
#
# ⚠️  WARNING: This drops and recreates the public schema.
#              All existing data in public.* will be overwritten.
#              Run this only after confirming with the team.
on:
  push:
    branches: [main, develop]
  workflow_dispatch:
    inputs:
      environment:
        description: "Target environment to restore INTO"
        required: false
        default: "dev"
        type: choice
        options:
          - dev
          - prod
      backup_file_path:
        description: "File path inside the 'backups' bucket (e.g. prod/prod-full-20260621T020000Z.sql.gz)"
        required: false
        default: ""
        type: string
      backup_log_id:
        description: "backup_logs.id to mark as the restore point (optional)"
        required: false
        default: ""
        type: string
      confirm:
        description: "Type RESTORE to confirm you want to overwrite the target database"
        required: false
        default: ""
        type: string

jobs:
  ci-push-noop:
    if: github.event_name == 'push'
    runs-on: ubuntu-latest
    steps:
      - run: echo "Push event — restore runs via manual dispatch or admin UI only."

  restore:
    if: github.event_name != 'push'
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      # ── safety guard ─────────────────────────────────────────────────────────
      - name: Confirm restore intent
        run: |
          CONFIRM=$(echo "${{ github.event.inputs.confirm }}" | tr -d '[:space:]' | tr '[:lower:]' '[:upper:]')
          if [ "$CONFIRM" != "RESTORE" ]; then
            echo "❌ Safety check failed. Type RESTORE exactly in the confirm field (got: '${{ github.event.inputs.confirm }}')."
            exit 1
          fi
          if [ -z "${{ github.event.inputs.backup_file_path }}" ]; then
            echo "❌ backup_file_path is required (e.g. dev/dev-snapshot-....sql.gz)."
            exit 1
          fi
          echo "RESTORE_STARTED_AT=$(date -u +%Y-%m-%dT%H:%M:%SZ)" >> "$GITHUB_ENV"

      # ── pick the right secrets ───────────────────────────────────────────────
      - name: Export environment-specific secrets
        run: |
          if [ "${{ github.event.inputs.environment }}" = "prod" ]; then
            echo "DB_URL=${{ secrets.SUPABASE_PROD_DB_URL }}"                >> "$GITHUB_ENV"
            echo "DB_PASSWORD=${{ secrets.SUPABASE_PROD_DB_PASSWORD }}"        >> "$GITHUB_ENV"
            echo "DB_POOLER_HOST=${{ secrets.SUPABASE_PROD_DB_POOLER_HOST }}"  >> "$GITHUB_ENV"
            echo "SUPABASE_URL=${{ secrets.SUPABASE_PROD_URL }}"             >> "$GITHUB_ENV"
            echo "SERVICE_KEY=${{ secrets.SUPABASE_PROD_SERVICE_ROLE_KEY }}" >> "$GITHUB_ENV"
          else
            echo "DB_URL=${{ secrets.SUPABASE_DEV_DB_URL }}"                 >> "$GITHUB_ENV"
            echo "DB_PASSWORD=${{ secrets.SUPABASE_DEV_DB_PASSWORD }}"       >> "$GITHUB_ENV"
            echo "DB_POOLER_HOST=${{ secrets.SUPABASE_DEV_DB_POOLER_HOST }}" >> "$GITHUB_ENV"
            echo "SUPABASE_URL=${{ secrets.SUPABASE_DEV_URL }}"              >> "$GITHUB_ENV"
            echo "SERVICE_KEY=${{ secrets.SUPABASE_DEV_SERVICE_ROLE_KEY }}"  >> "$GITHUB_ENV"
          fi

      - name: Configure PostgreSQL connection (IPv4)
        run: bash .github/scripts/configure-pg-ipv4.sh

      - name: Install matching PostgreSQL client
        run: bash .github/scripts/install-pg-client.sh

      # ── download the backup file from Supabase Storage ───────────────────────
      - name: Download backup file
        run: |
          FILE_PATH="${{ github.event.inputs.backup_file_path }}"
          FILENAME=$(basename "${FILE_PATH}")
          echo "FILENAME=${FILENAME}" >> "$GITHUB_ENV"

          HTTP_STATUS=$(curl -s -o "${FILENAME}" -w "%{http_code}" \
            "${SUPABASE_URL}/storage/v1/object/backups/${FILE_PATH}" \
            -H "Authorization: Bearer ${SERVICE_KEY}")

          if [ "$HTTP_STATUS" -lt 200 ] || [ "$HTTP_STATUS" -ge 300 ]; then
            echo "Failed to download backup (HTTP ${HTTP_STATUS})"
            exit 1
          fi
          echo "Downloaded ${FILENAME} ($(stat --format='%s' ${FILENAME}) bytes)"

      - name: Validate backup file
        run: |
          set -euo pipefail
          FILE_SIZE=$(stat --format='%s' "${FILENAME}")
          TABLE_COUNT=$(gunzip -c "${FILENAME}" | grep -c '^CREATE TABLE ' || true)
          echo "Backup file: ${FILE_SIZE} bytes, ${TABLE_COUNT} CREATE TABLE statements"
          if [ "$FILE_SIZE" -lt 500 ] || [ "$TABLE_COUNT" -eq 0 ]; then
            echo "Backup file looks empty or invalid."
            exit 1
          fi
          echo "RESTORE_FILE_SIZE=${FILE_SIZE}" >> "$GITHUB_ENV"
          echo "RESTORE_TABLE_COUNT=${TABLE_COUNT}" >> "$GITHUB_ENV"

      # ── restore ──────────────────────────────────────────────────────────────
      - name: Drop public schema
        run: |
          set -euo pipefail
          source /tmp/pg-connection.env
          psql -v ON_ERROR_STOP=1 -c "DROP SCHEMA IF EXISTS public CASCADE;"

      - name: Restore from backup
        id: restore
        run: |
          set -euo pipefail
          source /tmp/pg-connection.env
          set +e
          gunzip -c "${FILENAME}" \
            | sed '/^\\restrict /d; /^\\unrestrict /d' \
            | psql -v ON_ERROR_STOP=1 2>&1 | tee restore.log
          RESTORE_EXIT=${PIPESTATUS[1]}
          set -e
          if [ "$RESTORE_EXIT" -ne 0 ]; then
            echo "--- last 40 lines of restore.log ---"
            tail -n 40 restore.log || true
            exit "$RESTORE_EXIT"
          fi
          TABLE_COUNT=$(psql -tAc "select count(*) from information_schema.tables where table_schema = 'public';" | tr -d '[:space:]')
          echo "Public tables after restore: ${TABLE_COUNT}"
          if [ "${TABLE_COUNT}" -lt 1 ]; then
            echo "Restore produced an empty public schema."
            exit 1
          fi

      - name: Repair auth profile trigger and missing profiles
        if: steps.restore.outcome == 'success'
        run: |
          set -euo pipefail
          source /tmp/pg-connection.env
          psql -v ON_ERROR_STOP=1 -f supabase/post-restore-public-schema.sql

      # Audit log only — must not fail the restore job
      - name: Log restore to backup_logs
        if: steps.restore.outcome == 'success'
        continue-on-error: true
        run: |
          python3 - <<'PY'
          import json
          import os
          import subprocess
          import urllib.error
          import urllib.request

          supabase_url = os.environ["SUPABASE_URL"].rstrip("/")
          service_key = os.environ["SERVICE_KEY"]
          environment = os.environ["RESTORE_ENV"]
          file_path = os.environ["RESTORE_FILE_PATH"]
          run_url = os.environ["GITHUB_RUN_URL"]
          log_id = os.environ.get("RESTORE_LOG_ID", "").strip()
          started_at = os.environ["RESTORE_STARTED_AT"]
          file_size = int(os.environ.get("RESTORE_FILE_SIZE", "0"))
          table_count = int(os.environ.get("RESTORE_TABLE_COUNT", "0"))

          payload = {
              "backup_type": "snapshot",
              "operation": "restore",
              "environment": environment,
              "status": "completed",
              "triggered_by": "manual",
              "started_at": started_at,
              "completed_at": subprocess.check_output(
                  ["date", "-u", "+%Y-%m-%dT%H:%M:%SZ"], text=True
              ).strip(),
              "file_path": file_path,
              "file_size_bytes": file_size,
              "tables_dumped": table_count,
              "github_run_url": run_url,
          }

          headers = {
              "Authorization": f"Bearer {service_key}",
              "apikey": service_key,
              "Content-Type": "application/json",
              "Prefer": "return=representation",
          }

          def request(method, url, body=None):
              data = json.dumps(body).encode("utf-8") if body is not None else None
              req = urllib.request.Request(url, data=data, method=method, headers=headers)
              with urllib.request.urlopen(req) as resp:
                  return resp.status, resp.read()

          try:
              url = f"{supabase_url}/rest/v1/backup_logs"
              status, body = request("POST", url, payload)
              print(f"Inserted restore audit row (HTTP {status})")
              if log_id:
                  print(f"Restored from backup_log {log_id}")
          except urllib.error.HTTPError as err:
              body = err.read().decode("utf-8", errors="replace")
              print(f"Audit log skipped (HTTP {err.code}): {body}")
          PY
        env:
          SUPABASE_URL: ${{ env.SUPABASE_URL }}
          SERVICE_KEY: ${{ env.SERVICE_KEY }}
          RESTORE_ENV: ${{ github.event.inputs.environment }}
          RESTORE_FILE_PATH: ${{ github.event.inputs.backup_file_path }}
          RESTORE_LOG_ID: ${{ github.event.inputs.backup_log_id }}
          RESTORE_STARTED_AT: ${{ env.RESTORE_STARTED_AT }}
          RESTORE_FILE_SIZE: ${{ env.RESTORE_FILE_SIZE }}
          RESTORE_TABLE_COUNT: ${{ env.RESTORE_TABLE_COUNT }}
          GITHUB_RUN_URL: https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}

      - name: Sync backup_logs metadata (post-restore)
        if: steps.restore.outcome == 'success'
        continue-on-error: true
        run: python3 .github/scripts/sync-backup-logs-metadata.py
        env:
          SYNC_ENV: ${{ github.event.inputs.environment }}
          SUPABASE_URL: ${{ env.SUPABASE_URL }}
          SERVICE_KEY: ${{ env.SERVICE_KEY }}
          GITHUB_TOKEN: ${{ github.token }}
          GITHUB_REPOSITORY: ${{ github.repository }}
          LOCAL_BACKUP_FILE: ${{ env.FILENAME }}

      - name: Restore succeeded
        if: steps.restore.outcome == 'success'
        run: |
          echo "Restore completed. The public schema of '${{ github.event.inputs.environment }}' was replaced from '${{ github.event.inputs.backup_file_path }}'."

      - name: Restore failed
        if: steps.restore.outcome != 'success'
        run: |
          echo "Restore FAILED. The database may be in a partial state."
          echo "Re-run this workflow with a known-good backup file to recover."
