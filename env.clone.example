# Copy to env.clone.local (gitignored) for scripts/clone-prod-to-dev.ps1
# Never commit env.clone.local — it contains database passwords.

PROD_REF=ljfycjqawngzzrahyxsl
PROD_DB_PASSWORD=your-production-db-password

DEV_REF=your-dev-project-ref
DEV_DB_PASSWORD=your-dev-db-password
DEV_ANON_KEY=your-dev-anon-key
DEV_SERVICE_ROLE_KEY=your-dev-service-role-key
